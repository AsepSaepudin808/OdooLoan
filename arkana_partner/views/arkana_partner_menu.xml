<odoo>
    <data>
        <!-- <record id="menu_arkana_partner" model="ir.ui.menu">
            <field name="name">Arkana Partner</field>
            <field name="sequence" type="100"/>
            <field name="action" ref="arkana_partner.action_partner"/>
            <field name="groups_id" eval="[(6, 0, [base.group_user.id])]"/>
        </record> -->

        <!-- menu untuk memanggil Wizard -->
        <!-- <record id="action_partner_wizard" model="ir.actions.act_window">
            <field name="name">Select Categories and Products</field>
            <field name="res_model">arkana.partner.wizard</field>
            <field name="view_mode">form</field>
            <field name="view_id" ref="arkana_partner.view_partner_wizard_form"/>
            <field name="target">new</field>
        </record> -->

        <!-- <record id="menu_partner_wizard" model="ir.ui.menu">
            <field name="name">Select Categories and Products</field>
            <field name="parent_id" ref="arkana_partner.menu_arkana_partner"/>
            <field name="action" ref="arkana_partner.action_partner_wizard"/>
            <field name="groups_id" eval="[(6, 0, [base.group_user.id])]"/>
        </record> -->

        <!-- <menuitem
        id="action_partner_wizard"
        name="Wizard"
        parent="contacts.menu_contacts"
        action="action_partner_wizard"
        sequence="600"/> -->
    
    </data>
</odoo>